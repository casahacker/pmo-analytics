services:
  pmo-analytics:
    build: .
    container_name: casa-hacker-pmo-analytics
    restart: "no"
    ports:
      - "127.0.0.1:18087:3000"
    env_file:
      - .env
    volumes:
      - /data/pmo-analytics:/app/data:z
